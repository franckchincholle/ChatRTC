// Repository pour Server
// Accès base de données

export class ServerRepository {
  // TODO: CRUD operations
  // - create(data)
  // - findById(id)
  // - findByUserId(userId)
  // - update(id, data)
  // - delete(id)
  // - addMember(serverId, userId, role)
  // - removeMember(serverId, userId)
  // - updateMemberRole(serverId, userId, role)
}