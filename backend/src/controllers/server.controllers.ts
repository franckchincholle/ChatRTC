// Controller pour Server
// Handlers des requêtes HTTP

import { Request, Response } from 'express';

export class ServerController {
  // TODO: HTTP handlers
  // - POST /servers - createServer
  // - GET /servers - getUserServers
  // - GET /servers/:id - getServerById
  // - PUT /servers/:id - updateServer
  // - DELETE /servers/:id - deleteServer
  // - POST /servers/join - joinServerWithCode
  // - DELETE /servers/:id/leave - leaveServer
  // - POST /servers/:id/invite - generateInviteCode
  // - GET /servers/:id/members - getMembers
  // - PUT /servers/:id/members/:userId - updateMemberRole
}